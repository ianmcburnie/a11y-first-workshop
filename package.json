{
  "private": true,
  "scripts": {
    "start": "npm run build && browser-sync start -s --files *.html,*.css",
    "build": "lasso makeup-ebay main.js --minify --out . --name app && rimraf build"
  },
  "pre-commit": "build",
  "devDependencies": {
    "browser-sync": "^2",
    "jquery": ">=1.11",
    "lasso-cli": "^2.0.6",
    "makeup-ebay": "~0.24",
    "pre-commit": "^1.1.3",
    "rimraf": "^2.5.4"
  }
}
